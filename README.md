### Quake Entity Map Manager

A simple tool to extract and manage objects from Quake maps (e.g. it allows to remove objects from maps).
Usage (from the target directory):
ent-file-tool-1.0.jar InputFile
